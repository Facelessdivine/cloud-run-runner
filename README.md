# Playwright Distributed Runner (Cloud Run Jobs)

Runs Playwright tests from any repo using real distributed workers on Cloud Run Jobs.

Features:

- Dynamic sharding
- HTML + JUnit merge
- GCS upload
- Cleanup
- Signed URLs
- Runner outside test repo
